\begin{table}[h]
\begin{tabular}{|c|c|c|}
\hline
\textbf{\begin{tabular}[c]{@{}c@{}}actions\end{tabular}} &
\textbf{\begin{tabular}[c]{@{}c@{}}hash and \\ resubmit\end{tabular}} &
\textbf{\begin{tabular}[c]{@{}c@{}}merkle hash\\ and resubmit\end{tabular}} \\ \hline
\textit{hash} &
\textsf{H}($\data$) &
\begin{tabular}[c]{@{}c@{}}
\textsf{load}($\data$) +\\\textsf{H}(1)  $\times 2^{\lceil log_2(|\data| + 1) \rceil}$ \\
\end{tabular} \\ \hline
\textit{resubmit} &
\textsf{load}($\data$) + \textsf{H}($\data$) &
\begin{tabular}[c]{@{}c@{}}
    \textsf{load}($\data^{m:n})$ + \\
    \textsf{load}($\lceil log_2(|\data|) \rceil$) + \\
    \textsf{H}(1)$\times (\lceil log_2(|\data|) \rceil$)
\end{tabular} \\ \hline
\end{tabular}

\caption{Summary of operations for \emph{hash-and-resubmit} pattern variations.
    $\data$ is the product of on-chain operations. In the Merkle variation, a
    Merkle Tree Root is constructed on-chain during \emph{hash} phase. In
    \emph{resubmit} phase, the siblings of the Merkle Tree are dispatched, and
    the verification is performed on-chain. The number of siblings is
    approximately $\lceil log_2(|\data|) \rceil$.}

\label{tab:har-vs-mhar}
\end{table}

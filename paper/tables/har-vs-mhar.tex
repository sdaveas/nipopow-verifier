\begin{table}[h]
\begin{tabular}{|c|c|c|}
\hline
\textbf{\begin{tabular}[c]{@{}c@{}}actions\end{tabular}} &
\textbf{\begin{tabular}[c]{@{}c@{}}hash and \\ resubmit\end{tabular}} &
\textbf{\begin{tabular}[c]{@{}c@{}}merkle hash\\ and resubmit\end{tabular}} \\ \hline
\textit{hash} &
\textsf{load}($d_0$) + \textsf{H}($d$) &
\begin{tabular}[c]{@{}c@{}}
\textsf{load}($d_0$) +\\\textsf{H}(1)  $\times 2^{\lceil log_2(|d_0| + 1) \rceil}$ \\
\end{tabular} \\ \hline
\textit{resubmit} &
\textsf{load}($d$) + \textsf{H}($d$) &
\begin{tabular}[c]{@{}c@{}}
    \textsf{load}($d_0^{m:n})$ + \\
    \textsf{load}($\lceil log_2(|d_0^{m:n}|) \rceil$) + \\
    \textsf{H}(1)$\times (\lceil log_2(|d_0^{m:n}|) \rceil$)
\end{tabular} \\ \hline
\end{tabular}

\caption{Summary of operations for \emph{hash-and-resubmit} and
    \emph{merkle-hash-and-resubmit} pattern variations. $d_0$ is the data as
    sent from the caller. $d$ is the product of on-chain operations on $d_0$,
    and consist the subject of data comparison between the invoker and the
    observer of the function. For each variation, different gas cost functions
    are formulated. In the Merkle variation, a Merkle Tree Root is constructed
    on-chain during \emph{hash} phase. In \emph{resubmit} phase, the siblings
    of the Merkle Tree are dispatched, and the verification is performed
    on-chain.}

\label{tab:har-vs-mhar}
\end{table}
